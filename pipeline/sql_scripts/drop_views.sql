Drop view if EXISTS real_zipcodes cascade;

Drop view if EXISTS fhsz_zipcodes cascade;

Drop view if exists all_avg cascade;

drop view if exists avg_claims_losses cascade;

drop view if exists avg_fire_ppc cascade; 

Drop view if EXISTS zipcodes_exclude cascade;


drop MATERIALIZED view if exists fire_pp_claims_and_losses cascade;